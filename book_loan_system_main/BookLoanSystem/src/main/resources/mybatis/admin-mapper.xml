<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.book.mapper.admin.AdminMapper">
    <select id="login" resultType="string">
        SELECT `id` FROM `admin` WHERE `id` = #{id} AND `pw` = #{pw}
    </select>
</mapper>